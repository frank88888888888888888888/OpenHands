# OpenHands 简化后端使用指南

## 🎯 项目概述

这是一个基于 OpenHands 项目的简化后端版本，专门设计用于与 DeepSeek API 集成。它提供了一个命令行界面，让您可以通过输入任务描述来使用 AI Agent，并实时查看 Agent 的操作过程。

## 📁 项目文件

```
/workspace/project/
├── openhands_backend.py    # 完整版后端（需要真实API密钥）
├── simple_backend.py       # 简化版后端（需要真实API密钥）
├── demo_backend.py         # 演示版后端（无需API密钥）
├── config.py              # 配置文件
├── test_backend.py        # 测试脚本
├── README.md              # 英文说明文档
├── 使用指南.md             # 中文使用指南（本文件）
└── OpenHands/             # OpenHands 原始项目代码
```

## 🚀 快速开始

### 1. 演示模式（推荐新手）

无需任何配置，直接体验 AI Agent 的工作流程：

```bash
cd /workspace/project

# 创建计算器程序
python demo_backend.py "创建一个Python计算器程序"

# 制作网站
python demo_backend.py "制作一个简单的网站"

# 数据分析
python demo_backend.py "分析数据并生成报告"

# 自定义任务
python demo_backend.py "编写一个文件管理工具"
```

### 2. 真实模式（需要 DeepSeek API 密钥）

#### 获取 DeepSeek API 密钥
1. 访问 [DeepSeek 官网](https://platform.deepseek.com/)
2. 注册账号并获取 API 密钥
3. 设置环境变量：

```bash
export DEEPSEEK_API_KEY="your-deepseek-api-key-here"
```

#### 使用简化版后端

```bash
# 安装依赖
pip install httpx pydantic

# 运行任务
python simple_backend.py "创建一个Python计算器程序"
```

#### 使用完整版后端（需要完整的 OpenHands 环境）

```bash
# 安装 OpenHands（可能需要较长时间）
cd OpenHands
pip install -e .

# 返回项目目录
cd ..

# 运行任务
python openhands_backend.py "创建一个Python计算器程序"
```

## 🎭 演示效果

运行演示版本时，您会看到类似以下的实时输出：

```
🎭 OpenHands 演示后端启动中...
📝 注意：这是演示版本，使用模拟的AI响应

📋 任务: 创建一个Python计算器程序
🤖 模型: deepseek-chat (演示模式)
================================================================================

⚙️ [12:19:17] SYSTEM_MESSAGE
内容: 🚀 OpenHands AI Agent 启动
元数据: {
  "model": "deepseek-chat",
  "mode": "demo"
}
--------------------------------------------------------------------------------

👤 [12:19:17] USER_MESSAGE
内容: 创建一个Python计算器程序
--------------------------------------------------------------------------------

🧠 [12:19:18] AGENT_THINKING
内容: 我需要创建一个Python计算器程序。让我分析需求：
1. 支持基本四则运算
2. 用户友好的界面
3. 错误处理
4. 可扩展的设计
--------------------------------------------------------------------------------

⚡ [12:19:19] AGENT_ACTION
内容: 创建计算器主文件 calculator.py
--------------------------------------------------------------------------------

📁 [12:19:20] FILE_OPERATION
内容: 正在写入文件: calculator.py
元数据: {
  "file_path": "calculator.py",
  "operation": "create",
  "size": "1.2KB"
}
--------------------------------------------------------------------------------

... (更多事件)
```

## 🎯 支持的任务类型

### 编程任务
- "创建一个Python计算器程序"
- "编写一个文件管理工具"
- "开发一个简单的游戏"
- "制作一个API服务器"

### 网站开发
- "制作一个简单的网站"
- "创建一个博客页面"
- "开发一个在线工具"
- "设计一个响应式网页"

### 数据处理
- "分析数据并生成报告"
- "处理CSV文件"
- "创建数据可视化图表"
- "执行统计分析"

### 通用任务
- "帮我整理项目文档"
- "优化代码性能"
- "修复程序bug"
- "编写测试用例"

## 🔧 配置选项

### 模型选择

支持多种 DeepSeek 模型：

```bash
# 使用默认模型（deepseek-chat）
python simple_backend.py "任务描述"

# 指定特定模型
python simple_backend.py "任务描述" deepseek-coder
python simple_backend.py "任务描述" deepseek-reasoner
```

### 自定义配置

编辑 `config.py` 文件来调整：

```python
# DeepSeek API 配置
DEEPSEEK_CONFIG = {
    "api_key": os.getenv("DEEPSEEK_API_KEY", ""),
    "base_url": "https://api.deepseek.com",
    "models": {
        "deepseek-chat": {
            "temperature": 0.1,
            "max_tokens": 4096,
        },
        # 添加更多模型配置...
    }
}
```

## 📊 事件类型说明

系统会输出以下类型的事件：

| 图标 | 事件类型 | 说明 |
|------|----------|------|
| 👤 | USER_MESSAGE | 用户输入的任务 |
| 🤖 | AGENT_MESSAGE | AI Agent 的最终回复 |
| 🧠 | AGENT_THINKING | AI Agent 的思考过程 |
| ⚡ | AGENT_ACTION | AI Agent 执行的操作 |
| 👁️ | AGENT_OBSERVATION | AI Agent 观察到的结果 |
| 📁 | FILE_OPERATION | 文件操作（创建、修改、删除） |
| 💻 | CODE_EXECUTION | 代码执行 |
| ⚙️ | SYSTEM_MESSAGE | 系统消息 |
| ❌ | ERROR | 错误信息 |

## 🛠️ 故障排除

### 常见问题

1. **API 密钥错误**
   ```
   错误: 请设置环境变量 DEEPSEEK_API_KEY
   ```
   **解决方案**: 确保正确设置了环境变量

2. **依赖缺失**
   ```
   ModuleNotFoundError: No module named 'httpx'
   ```
   **解决方案**: 运行 `pip install httpx pydantic`

3. **网络连接问题**
   ```
   DeepSeek API 请求失败: Connection timeout
   ```
   **解决方案**: 检查网络连接，或使用演示模式

### 调试模式

如需查看详细的调试信息，可以修改代码中的日志级别：

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

## 🎨 自定义开发

### 添加新的事件类型

1. 在 `EventType` 枚举中添加新类型
2. 在 `EventPrinter` 中添加对应的图标
3. 在 Agent 中使用新的事件类型

### 扩展 Agent 功能

1. 修改 `MockAgent` 类添加新的任务处理逻辑
2. 在 `process_task` 方法中添加任务类型判断
3. 创建对应的步骤生成方法

### 集成真实工具

将演示版本的模拟操作替换为真实的工具调用：

```python
# 替换模拟的文件操作
async def create_file(self, path: str, content: str):
    with open(path, 'w', encoding='utf-8') as f:
        f.write(content)

    self.event_stream.emit_event(
        EventType.FILE_OPERATION,
        f"文件创建成功: {path}",
        {"path": path, "size": len(content)}
    )
```

## 📈 性能优化

### 减少延迟

在演示模式中，可以调整步骤之间的延迟时间：

```python
# 在 _create_calculator_steps 等方法中
{
    "type": "agent_thinking",
    "content": "思考内容...",
    "delay": 0.1  # 减少延迟时间
}
```

### 批量处理

对于多个任务，可以创建批量处理脚本：

```python
tasks = [
    "创建计算器",
    "制作网站",
    "分析数据"
]

for task in tasks:
    result = await backend.process_task(task)
    print(f"任务 '{task}' 完成: {result['success']}")
```

## 🤝 贡献指南

欢迎提交改进建议和代码贡献：

1. Fork 项目
2. 创建功能分支
3. 提交更改
4. 发起 Pull Request

## 📄 许可证

本项目基于 OpenHands 项目，遵循相同的开源许可证。

## 🆘 获取帮助

如有问题或建议：

1. 查看本使用指南
2. 运行演示模式熟悉功能
3. 检查错误日志
4. 提交 GitHub Issue

---

**祝您使用愉快！🎉**
